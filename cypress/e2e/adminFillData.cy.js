describe('To add category, head category, sub category, image, and product', () => {
  it('should convert Ecel file to .json file', () => {});

  it('should add three categories', () => {
    cy.visit(Cypress.env('url'));
  });
});
